# API Configuration
# Set the base URL for API calls. If not set, defaults to relative paths (proxied by Vite dev server)
# For production, set to your backend URL (e.g., https://api.galaretkarnia.pl)
VITE_API_BASE_URL=http://localhost:3000
